# Membership only. Everything descriptive about this library — label,
# description, tags, version, authors — is read out of the wheel at generation
# time and is NOT authored here. See README.md, "What being in this catalogue
# means".
name    = "haybale-visiongraph"   # the PyPI distribution name
repo    = "https://github.com/going-haywire/haybale-visiongraph"
contact = "maybites"
added   = "2026-08-28"
